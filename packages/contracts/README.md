# `@microsonya/contracts`

Portable internal service contracts for Microsonya. This package may depend on
the domain model, but it must not import Cloudflare, Wrangler, Telegram, Telegraf,
HTTP, or database types.
